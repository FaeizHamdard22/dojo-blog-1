const SearchBox = () => {
  return (
    <div className="search">
      <input
        type="search"
        placeholder="Search"
        className="bg-light form-control"
      />
    </div>
  );
};

export default SearchBox;
